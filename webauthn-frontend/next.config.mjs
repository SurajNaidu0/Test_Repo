/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add any Next.js-specific configuration here if needed
    reactStrictMode: true, // Optional: Enables React strict mode
};

export default nextConfig;